# data branch — odds snapshot storage

This branch exists only to version `odds.sqlite`, the append-only SQLite
snapshot database written by `.github/workflows/snapshot.yml`.

Do not edit by hand; every commit here is a scheduled odds snapshot.
